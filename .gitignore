node_modules/
dist/

# Env files — nunca subir, en ningún subdirectorio
.env
.env.local
.env.*.local
**/.env
**/.env.local
**/.env.*.local

# XMTP local database — contiene salt de cifrado, nunca subir
**/*.db3
**/*.db3-shm
**/*.db3-wal
**/*.db3.sqlcipher_salt

# Hardhat — compilados y despliegues con addresses privadas
packages/contracts/cache/
packages/contracts/artifacts/
packages/contracts/deployments/
packages/contracts/typechain-types/

# UI build
packages/ui/dist/

# Estado interno del agente (memoria de Claude Code)
memory/
